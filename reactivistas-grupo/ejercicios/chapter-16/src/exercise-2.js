// Create another version of the app from Exercise 1, with labelled text inputs for First Nameand Last Name, but make themuncontrolledinputs instead. Create refs to the inputs, and add aSubmit button that, when clicked, will update the display to reflect the values in the inputs.
import React, { useRef } from 'react';
import ReactDOM from 'react-dom';

const UncontrolledHelloApp = () => {

  return (
    <div>
    
    </div>
  );
};

ReactDOM.render(<UncontrolledHelloApp/>, document.getElementById('root'));
