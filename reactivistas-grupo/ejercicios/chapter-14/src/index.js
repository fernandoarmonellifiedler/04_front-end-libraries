// Exercise 1:
// Create a Room component with a “lightswitch” button and some text describing “The room islit” or “The room is dark”. Clicking the button should toggle the light on and off, and updatethe text. Use the useState hook to store the lightswitch state. 
import React, { useState } from 'react';
import ReactDOM from 'react-dom';

function Room() {

  return (
    <>
      
    </>
  );
}

ReactDOM.render(<Room />, document.getElementById('root'));
